// Leaflet MapView — will be integrated in Phase 2
export default function MapView({ center, markers = [] }) {
  return (
    <div className="w-full h-64 sm:h-96 card-glass flex items-center justify-center text-white/30">
      <p className="text-sm">Map will render here (Phase 2)</p>
    </div>
  );
}
